// Example for quiz interactivity
document.querySelectorAll('.quiz-button').forEach(button => {
  button.addEventListener('click', () => {
    alert('Thanks for participating in the quiz!');
  });
});
